@model IEnumerable<JQueryAjaxCRUDInCore.Models.CTransaction>

<div class="col-md-8 offset-md-2">

    <h1 class="text-center"><i class="fas fa-comments-dollar text-success"></i> Transaction History</h1>


    <table class="table">
        <thead class="thead-light">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.fAccountNum)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.fBeneficiaryName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.fDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.fAmount)
                </th>
                <th>
                    <a onclick="showInPopup('@Url.Action("AddOrEdit","Transaction",null,Context.Request.Scheme)','New Transaction')" class="btn btn-success text-white"><i class="fas fa-random"></i> New Transaction</a>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.fAccountNum)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.fBeneficiaryName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.fDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.fAmount)
                    </td>
                    <td>
                        <div>
                            <a onclick="showInPopup('@Url.Action("AddOrEdit","Transaction",new {id=item.fTranId},Context.Request.Scheme)','Update Transaction')" class="btn btn-info text-white"><i class="fas fa-pencil-alt"></i> Edit</a>
                            <form asp-action="Delete" asp-route-id="@item.fTranId" onsubmit="return jQueryAjaxDelete(this)" class="d-inline">
                                <input type="hidden" asp-for="@item.fTranId" />
                                <input type="submit" value="Delete" class="btn btn-danger" />
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>